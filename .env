# .env
REACT_APP_API_URL=http://localhost:3000/api
REACT_APP_LOGIN_PATH=/users/login
# If your backend uses httpOnly cookie sessions (no token in JSON), uncomment:
# REACT_APP_AUTH_MODE=cookie

# Optional: for low-stock card on the dashboard
REACT_APP_LOW_STOCK=10